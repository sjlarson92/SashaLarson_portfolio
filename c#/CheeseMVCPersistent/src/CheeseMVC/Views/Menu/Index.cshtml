@model List<CheeseMVC.Models.CheeseMenu>

<h1>@ViewBag.Title</h1>

<table class="table">

    <tr>
        <th>MenuID</th>
        <th>Menu</th>
        <td>CheeseID</td>
        <td>Cheese</td>
    </tr>

    @foreach (var menu in Model)
    {
        <tr>
            <td>@menu.MenuID</td>
            <td>@menu.Menu</td>
            <td>@menu.CheeseID</td>
            <td>@menu.Cheese</td>
        </tr>
    }
</table>

@if (Model.Count == 0)
{
    <p>No menus yet!</p>
}

<p><a asp-controller="Cheese" asp-action="Add">Add Cheese</a></p>
