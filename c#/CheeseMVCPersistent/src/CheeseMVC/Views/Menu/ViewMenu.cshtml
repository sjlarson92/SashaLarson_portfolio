@model CheeseMVC.ViewModels.ViewMenuViewModel

<h1>@ViewBag.Title</h1>

<table class="table">

    @foreach (var items in Model.Items)
    {
      <ul>
        @cheeseMenu.Cheeses.Name
      </ul>
    }
</table>
